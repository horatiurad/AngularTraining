import { TazzOrderBillingInfo } from './tazz-order-billing-info';

describe('TazzOrderBillingInfo', () => {
  it('should create an instance', () => {
    expect(new TazzOrderBillingInfo()).toBeTruthy();
  });
});
